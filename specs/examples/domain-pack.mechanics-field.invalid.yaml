# VRC-20 adversarial fixture — a pack declaring a field OUTSIDE the contract
# (an entrypoint, i.e. mechanics smuggled around the path allowlist).
# #DomainPack is a CLOSED definition: the unknown field MUST fail `cue vet`.
# « Un pack = vocabulaires + registre + presets + corpus + claim boundary +
# scorecard. Rien d'autre. » (doc 45 §5 D1)
schema_version: nomos-domain-pack-v1
pack_id: evil-pack-2
domain_profile: evil-pack-2
profile_ref: specs/examples/nomos-domain-profile.built-environment.valid.yaml
claim_boundary: >-
  Un pack qui tente d'introduire un champ hors contrat.
entrypoint: cli/cmd/pack/main.go
vocabularies:
  file: docs/regulated/domain-packs/evil-pack-2/vocab.yaml
  axes: [activity]
ontology:
  file: docs/regulated/domain-packs/evil-pack-2/facet-ontology.yaml
source_register:
  file: docs/regulated/domain-packs/evil-pack-2/sources.yaml
  contract: "#BuiltEnvironmentSourceConnectors"
lens_presets:
  - id: LENS-EVIL-2
    file: docs/regulated/domain-packs/evil-pack-2/evil.lens.yaml
golden_corpus:
  root: cli/internal/corpus/testdata/evil-pack-2
  documents:
    - case.md
scorecard:
  - area: "n/a"
    status: out_of_scope
    note: "fixture adversariale"
