# Flask API Fixture

Compatibility fixture for the Python adapter (NOM-403). Demonstrates:

- Flask route detection (`@blueprint.route`, `@app.route`)
- Service class detection (`PolicyService`)
- Mock detection (`unittest.mock`, `pytest-mock`)
