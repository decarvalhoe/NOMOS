# Django App Fixture

Compatibility fixture for the Python adapter (NOM-403). Demonstrates:

- Django URL pattern detection (`urlpatterns`, `path()`)
- DRF serializer detection (`ModelSerializer`)
- Class-based view detection
- Service class detection (`PolicyService`)
- Django fixture detection (`fixtures/*.json`)
